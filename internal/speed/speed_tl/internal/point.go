// Code generated by tl2gen; DO NOT EDIT.
package internal

import (
	"github.com/VKCOM/tl/internal/speed/speed_tl/basictl"
)

var _ = basictl.NatWrite

func BuiltinVectorPointBoxedReadTL1(w []byte, vec *[]Point) (_ []byte, err error) {
	var l uint32
	if w, err = basictl.NatRead(w, &l); err != nil {
		return w, err
	}
	if err = basictl.CheckLengthSanity(w, l, 4); err != nil {
		return w, err
	}
	if uint32(cap(*vec)) < l {
		*vec = make([]Point, l)
	} else {
		*vec = (*vec)[:l]
	}
	for i := range *vec {
		if w, err = (*vec)[i].ReadTL1Boxed(w); err != nil {
			return w, err
		}
	}
	return w, nil
}

func BuiltinVectorPointBoxedWriteTL1(w []byte, vec []Point) []byte {
	w = basictl.NatWrite(w, uint32(len(vec)))
	for _, elem := range vec {
		w = elem.WriteTL1Boxed(w)
	}
	return w
}

func BuiltinVectorPointBoxedCalculateLayout(sizes []int, optimizeEmpty bool, vec *[]Point) ([]int, int) {
	if len(*vec) == 0 {
		if optimizeEmpty {
			return sizes, 0
		}
		return sizes, 1
	}
	sizePosition := len(sizes)
	sizes = append(sizes, 0)

	currentSize := 0
	var sz int

	currentSize += basictl.TL2CalculateSize(len(*vec))
	for i := 0; i < len(*vec); i++ {
		sizes, sz = (*vec)[i].CalculateLayout(sizes, false)
		currentSize += sz
	}
	sizes[sizePosition] = currentSize
	currentSize += basictl.TL2CalculateSize(currentSize)
	Unused(sz)
	return sizes, currentSize
}

func BuiltinVectorPointBoxedInternalWriteTL2(w []byte, sizes []int, optimizeEmpty bool, vec *[]Point) ([]byte, []int, int) {
	if len(*vec) == 0 {
		if optimizeEmpty {
			return w, sizes, 0
		}
		w = append(w, 0)
		return w, sizes, 1
	}
	currentSize := sizes[0]
	sizes = sizes[1:]
	w = basictl.TL2WriteSize(w, currentSize)
	if currentSize == 0 {
		return w, sizes, 1
	}
	oldLen := len(w)
	w = basictl.TL2WriteSize(w, len(*vec))

	var sz int
	for i := 0; i < len(*vec); i++ {
		w, sizes, _ = (*vec)[i].InternalWriteTL2(w, sizes, false)
	}
	Unused(sz)
	if len(w)-oldLen != currentSize {
		panic("tl2: mismatch between calculate and write")
	}
	return w, sizes, currentSize
}

func BuiltinVectorPointBoxedInternalReadTL2(r []byte, vec *[]Point) (_ []byte, err error) {
	currentSize := 0
	if r, currentSize, err = basictl.TL2ParseSize(r); err != nil {
		return r, err
	}
	if len(r) < currentSize {
		return r, basictl.TL2Error("not enough data: expected %d, got %d", currentSize, len(r))
	}

	currentR := r[:currentSize]
	r = r[currentSize:]

	elementCount := 0
	if currentSize != 0 {
		if currentR, elementCount, err = basictl.TL2ParseSize(currentR); err != nil {
			return r, err
		}
		if elementCount > len(currentR) {
			return r, basictl.TL2ElementCountError(elementCount, currentR)
		}
	}

	if cap(*vec) < elementCount {
		*vec = make([]Point, elementCount)
	}
	*vec = (*vec)[:elementCount]
	for i := 0; i < elementCount; i++ {
		if currentR, err = (*vec)[i].InternalReadTL2(currentR); err != nil {
			return currentR, err
		}
	}
	return r, nil
}

func BuiltinVectorPointBoxedReadJSONGeneral(jctx *basictl.JSONReadContext, in *basictl.JsonLexer, vec *[]Point) error {
	*vec = (*vec)[:cap(*vec)]
	index := 0
	if in != nil {
		in.Delim('[')
		if !in.Ok() {
			return ErrorInvalidJSON("[]Point", "expected json array")
		}
		for ; !in.IsDelim(']'); index++ {
			if len(*vec) <= index {
				var newValue Point
				*vec = append(*vec, newValue)
				*vec = (*vec)[:cap(*vec)]
			}
			if err := (*vec)[index].ReadJSONGeneral(jctx, in); err != nil {
				return err
			}
			in.WantComma()
		}
		in.Delim(']')
		if !in.Ok() {
			return ErrorInvalidJSON("[]Point", "expected json array's end")
		}
	}
	*vec = (*vec)[:index]
	return nil
}

func BuiltinVectorPointBoxedWriteJSON(w []byte, vec []Point) []byte {
	return BuiltinVectorPointBoxedWriteJSONOpt(nil, w, vec)
}
func BuiltinVectorPointBoxedWriteJSONOpt(jctx *basictl.JSONWriteContext, w []byte, vec []Point) []byte {
	w = append(w, '[')
	for _, elem := range vec {
		w = basictl.JSONAddCommaIfNeeded(w)
		w = elem.WriteJSONOpt(jctx, w)
	}
	return append(w, ']')
}

type Point struct {
	X uint32
	Y uint32
	Z uint32
}

func (Point) TLName() string { return "point" }
func (Point) TLTag() uint32  { return 0x8ec1a27e }

func (item *Point) Reset() {
	item.X = 0
	item.Y = 0
	item.Z = 0
}

func (item *Point) ReadTL1(w []byte) (_ []byte, err error) {
	if w, err = basictl.NatRead(w, &item.X); err != nil {
		return w, err
	}
	if w, err = basictl.NatRead(w, &item.Y); err != nil {
		return w, err
	}
	return basictl.NatRead(w, &item.Z)
}

func (item *Point) WriteTL1General(w []byte) (_ []byte, err error) {
	return item.WriteTL1(w), nil
}

func (item *Point) WriteTL1(w []byte) []byte {
	w = basictl.NatWrite(w, item.X)
	w = basictl.NatWrite(w, item.Y)
	w = basictl.NatWrite(w, item.Z)
	return w
}

func (item *Point) ReadTL1Boxed(w []byte) (_ []byte, err error) {
	if w, err = basictl.NatReadExactTag(w, 0x8ec1a27e); err != nil {
		return w, err
	}
	return item.ReadTL1(w)
}

func (item *Point) WriteTL1BoxedGeneral(w []byte) (_ []byte, err error) {
	return item.WriteTL1Boxed(w), nil
}

func (item *Point) WriteTL1Boxed(w []byte) []byte {
	w = basictl.NatWrite(w, 0x8ec1a27e)
	return item.WriteTL1(w)
}

func (item Point) String() string {
	return string(item.WriteJSON(nil))
}

func (item *Point) ReadJSON(legacyTypeNames bool, in *basictl.JsonLexer) error {
	jctx := basictl.JSONReadContext{LegacyTypeNames: legacyTypeNames}
	return item.ReadJSONGeneral(&jctx, in)
}

func (item *Point) ReadJSONGeneral(jctx *basictl.JSONReadContext, in *basictl.JsonLexer) error {
	var propXPresented bool
	var propYPresented bool
	var propZPresented bool
	if in != nil {
		in.Delim('{')
		if !in.Ok() {
			return in.Error()
		}
		for !in.IsDelim('}') {
			key := in.UnsafeFieldName(true)
			in.WantColon()
			switch key {
			case "x":
				if propXPresented {
					return ErrorInvalidJSONWithDuplicatingKeys("point", "x")
				}
				propXPresented = true
				if err := Json2ReadUint32(in, &item.X); err != nil {
					return err
				}
			case "y":
				if propYPresented {
					return ErrorInvalidJSONWithDuplicatingKeys("point", "y")
				}
				propYPresented = true
				if err := Json2ReadUint32(in, &item.Y); err != nil {
					return err
				}
			case "z":
				if propZPresented {
					return ErrorInvalidJSONWithDuplicatingKeys("point", "z")
				}
				propZPresented = true
				if err := Json2ReadUint32(in, &item.Z); err != nil {
					return err
				}
			default:
				return ErrorInvalidJSONExcessElement("point", key)
			}
			in.WantComma()
		}
		in.Delim('}')
		if !in.Ok() {
			return in.Error()
		}
	}
	if !propXPresented {
		item.X = 0
	}
	if !propYPresented {
		item.Y = 0
	}
	if !propZPresented {
		item.Z = 0
	}
	return nil
}

// This method is general version of WriteJSON, use it instead!
func (item *Point) WriteJSONGeneral(jctx *basictl.JSONWriteContext, w []byte) (_ []byte, err error) {
	return item.WriteJSONOpt(jctx, w), nil
}

func (item *Point) WriteJSON(w []byte) []byte {
	return item.WriteJSONOpt(nil, w)
}
func (item *Point) WriteJSONOpt(jctx *basictl.JSONWriteContext, w []byte) []byte {
	w = append(w, '{')
	backupIndexX := len(w)
	w = basictl.JSONAddCommaIfNeeded(w)
	w = append(w, `"x":`...)
	w = basictl.JSONWriteUint32(w, item.X)
	if !(item.X != 0) {
		w = w[:backupIndexX]
	}
	backupIndexY := len(w)
	w = basictl.JSONAddCommaIfNeeded(w)
	w = append(w, `"y":`...)
	w = basictl.JSONWriteUint32(w, item.Y)
	if !(item.Y != 0) {
		w = w[:backupIndexY]
	}
	backupIndexZ := len(w)
	w = basictl.JSONAddCommaIfNeeded(w)
	w = append(w, `"z":`...)
	w = basictl.JSONWriteUint32(w, item.Z)
	if !(item.Z != 0) {
		w = w[:backupIndexZ]
	}
	return append(w, '}')
}

func (item *Point) MarshalJSON() ([]byte, error) {
	return item.WriteJSON(nil), nil
}

func (item *Point) UnmarshalJSON(b []byte) error {
	jctx := basictl.JSONReadContext{LegacyTypeNames: true}
	if err := item.ReadJSONGeneral(&jctx, &basictl.JsonLexer{Data: b}); err != nil {
		return ErrorInvalidJSON("point", err.Error())
	}
	return nil
}

func (item *Point) CalculateLayout(sizes []int, optimizeEmpty bool) ([]int, int) {
	sizes = append(sizes, 616939567)
	sizePosition := len(sizes)
	sizes = append(sizes, 0)

	currentSize := 1
	lastUsedByte := 0
	var sz int

	if item.X != 0 {
		currentSize += 4
		lastUsedByte = currentSize
	}
	if item.Y != 0 {
		currentSize += 4
		lastUsedByte = currentSize
	}
	if item.Z != 0 {
		currentSize += 4
		lastUsedByte = currentSize
	}

	if lastUsedByte < currentSize {
		currentSize = lastUsedByte
	}
	sizes[sizePosition] = currentSize
	if currentSize == 0 {
		sizes = sizes[:sizePosition+1]
	}
	if !optimizeEmpty || currentSize != 0 {
		currentSize += basictl.TL2CalculateSize(currentSize)
	}
	Unused(sz)
	return sizes, currentSize
}

func (item *Point) InternalWriteTL2(w []byte, sizes []int, optimizeEmpty bool) ([]byte, []int, int) {
	if sizes[0] != 616939567 {
		panic("tl2: tag mismatch between calculate and write")
	}
	currentSize := sizes[1]
	sizes = sizes[2:]
	if optimizeEmpty && currentSize == 0 {
		return w, sizes, 0
	}
	w = basictl.TL2WriteSize(w, currentSize)
	if currentSize == 0 {
		return w, sizes, 1
	}
	oldLen := len(w)
	var sz int
	var currentBlock byte
	currentBlockPosition := len(w)
	w = append(w, 0)
	if item.X != 0 {
		w = basictl.NatWrite(w, item.X)
		currentBlock |= 2
	}
	if item.Y != 0 {
		w = basictl.NatWrite(w, item.Y)
		currentBlock |= 4
	}
	if item.Z != 0 {
		w = basictl.NatWrite(w, item.Z)
		currentBlock |= 8
	}
	if currentBlockPosition < len(w) {
		w[currentBlockPosition] = currentBlock
	}
	if len(w)-oldLen != currentSize {
		panic("tl2: mismatch between calculate and write")
	}
	Unused(sz)
	return w, sizes, 1
}

func (item *Point) WriteTL2(w []byte, tctx *basictl.TL2WriteContext) []byte {
	var sizes, sizes2 []int
	if tctx != nil {
		sizes = tctx.SizeBuffer[:0]
	}
	sizes, _ = item.CalculateLayout(sizes, false)
	w, sizes2, _ = item.InternalWriteTL2(w, sizes, false)
	if len(sizes2) != 0 {
		panic("tl2: internal write did not consume all size data")
	}
	if tctx != nil {
		tctx.SizeBuffer = sizes
	}
	return w
}

func (item *Point) InternalReadTL2(r []byte) (_ []byte, err error) {
	currentSize := 0
	if r, currentSize, err = basictl.TL2ParseSize(r); err != nil {
		return r, err
	}
	if currentSize == 0 {
		item.Reset()
		return r, nil
	}
	if len(r) < currentSize {
		return r, basictl.TL2Error("not enough data: expected %d, got %d", currentSize, len(r))
	}

	currentR := r[:currentSize]
	r = r[currentSize:]

	var block byte
	if currentR, err = basictl.ByteRead(currentR, &block); err != nil {
		return currentR, err
	}
	// read No of constructor
	if block&1 != 0 {
		var index int
		if currentR, index, err = basictl.TL2ParseSize(currentR); err != nil {
			return currentR, err
		}
		if index != 0 {
			return r, ErrorInvalidUnionIndex("point", index)
		}
	}
	if block&2 != 0 {
		if currentR, err = basictl.NatRead(currentR, &item.X); err != nil {
			return currentR, err
		}
	} else {
		item.X = 0
	}
	if block&4 != 0 {
		if currentR, err = basictl.NatRead(currentR, &item.Y); err != nil {
			return currentR, err
		}
	} else {
		item.Y = 0
	}
	if block&8 != 0 {
		if currentR, err = basictl.NatRead(currentR, &item.Z); err != nil {
			return currentR, err
		}
	} else {
		item.Z = 0
	}
	Unused(currentR)
	return r, nil
}

func (item *Point) ReadTL2(r []byte, tctx *basictl.TL2ReadContext) (_ []byte, err error) {
	return item.InternalReadTL2(r)
}
